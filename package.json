{
  "name": "@payloadcms/db-mongoose",
  "description": "The official Mongoose / MongoDB adapter for Payload",
  "homepage": "https://github.com/payloadcms/db-mongoose",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src",
    "lint:fix": "eslint --fix --ext .ts,.tsx src",
    "clean": "rimraf dist && rimraf dev/yarn.lock",
    "prepublishOnly": "yarn clean && yarn build"
  },
  "peerDependencies": {
    "payload": "^1.8.5"
  },
  "files": [
    "dist",
    "*.js",
    "*.d.ts",
    "!.prettierrc.js"
  ],
  "devDependencies": {
    "@payloadcms/eslint-config": "^0.0.1",
    "@typescript-eslint/eslint-plugin": "5.12.1",
    "@typescript-eslint/parser": "5.12.1",
    "eslint": "^8.19.0",
    "prettier": "^2.7.1",
    "rimraf": "^4.1.2",
    "ts-node": "^10.9.1",
    "typescript": "^4.1.3"
  }
}